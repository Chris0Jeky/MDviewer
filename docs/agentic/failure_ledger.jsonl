{"date": "2026-06-25", "class": "non_blocking_risk", "surface": "scaffold", "failure": "Bootstrap entry — ledger initialized with the project scaffold.", "workaround": "n/a", "future_fix": "Replace with the first real finding.", "status": "closed"}
{"date": "2026-08-16", "class": "non_blocking_risk", "surface": "render", "failure": "SLUGIFY stays ASCII-only: unicode-aware slugs would percent-encode into ids that handler.ts fillTocPageNumbers can no longer resolve after decodeURIComponent, silently killing preview TOC page numbers. Headings differing only in non-ASCII rely on markdown-it-anchor dedupe (-1 suffixes).", "workaround": "TOC text itself is fixed (headingText unwraps the headerLink permalink); a buildSource.test.ts test documents the ASCII-slug trade-off.", "future_fix": "Make fillTocPageNumbers resolve encoded ids (or switch lookups to CSS.escape) before making SLUGIFY unicode-aware.", "status": "open"}
{"date": "2026-08-16", "class": "non_blocking_risk", "surface": "pagination", "failure": "QA UX-10 (first half): keep-whole blocks can leave large trailing whitespace before a pushed block. Structural trade-off of the no-slice guarantee; the running-header half (wrong section title on pushed content) was fixed via string(doctitle, start).", "workaround": "Accepted as-is; shrink tier deliberately untouched (never shrink reflowing tables; SHRINK_LIMIT 1.15).", "future_fix": "If revisited, only levers are SHRINK_SELECTOR/SHRINK_LIMIT in afterParsed, re-proving both nocutoff e2e tests.", "status": "open"}
{"date": "2026-08-16", "class": "non_blocking_risk", "surface": "export-ui", "failure": "The export progress overlay is invisible in Markdown-only view mode because editor.css parks #canvas with visibility:hidden; disabled export buttons remain the only feedback there.", "workaround": "Buttons disable + aria-live announcements still fire in every view mode.", "future_fix": "Surface export progress in toolbar/status chrome outside #canvas, or unhide a minimal progress element in Markdown mode.", "status": "open"}
{"date": "2026-08-16", "class": "non_blocking_risk", "surface": "pwa", "failure": "PWA specifics unverified against the live Cloudflare deployment: /workbox-*.js splat rule in _headers, real-redeploy update-toast flow (verified only against a simulated redeploy on the local static server), and static light theme-color for dark-theme users.", "workaround": "Local preview-target e2e proves offline + precache; deploy smoke checklist extended in docs/DEPLOYMENT.md.", "future_fix": "After next deploy: verify sw.js/manifest Cache-Control, workbox header match, install + offline + update-toast in a real browser (tracked as a manual gate in ACTION_ITEMS).", "status": "open"}
{"date": "2026-08-16", "class": "non_blocking_risk", "surface": "ui", "failure": "QA TECH-1 (High) deferred: no width-based media query in app.css; the toolbar (~7 groups, ~25 controls) overflows/stacks on tablet/phone widths. Session was wrapped up before implementation.", "workaround": "Desktop widths fixed (.toolbar-field layout, spacer right-aligns Export); the split workspace already stacks at the 720px breakpoint in editor.css.", "future_fix": "Reuse the 720px breakpoint (never a second one): at <=720px make #toolbar a single scrollable strip - flex-wrap:nowrap, overflow-x:auto, overflow-y:hidden, overscroll-behavior-x:contain, min-width:0 on #toolbar and .toolbar-group (required: #app is an overflow-hidden grid), hide .toolbar-divider, narrow the header input and .doc-switcher, shrink --ctl-h to ~28px scoped to #toolbar, keep >=6px vertical padding so the 2px focus ring is not clipped. Add tests/e2e/responsive.spec.ts at 375x667 and 768px: documentElement.scrollWidth <= innerWidth, toolbar height cap, clickable export buttons, sample still paginates.", "status": "open"}
{"date": "2026-08-16", "class": "non_blocking_risk", "surface": "pagination", "failure": "string(doctitle, start) is unit-asserted only; no e2e proves the running header names the previous section for pushed content, and titlePage:false was not exercised end-to-end.", "workaround": "Both are pinned by cssBuilder unit tests; manual check added to the AI-7 gate.", "future_fix": "E2E fixture engineered to push a tall block onto a new-heading page, asserting the .pagedjs_margin-top-right content; plus a titlePage:false e2e asserting page-1 chrome.", "status": "open"}
